# Hartto — regras do servidor (Hostinger)
ErrorDocument 404 /404.html
RewriteEngine On
# heranças do WordPress antigo: qualquer URL da era index.php vai para a home
RewriteRule ^index\.php(/.*)?$ https://www.hartto.com/ [R=301,L]
RewriteRule ^(wp-content|wp-admin|wp-includes|feed)(/.*)?$ https://www.hartto.com/ [R=301,L]
# sobras da v1 descobertas na auditoria de 19/08: 301 para destinos atuais
RewriteRule ^compatibilizacao-projetos-bim/?$ https://www.hartto.com/projeto-estrutural-para-construtoras/ [R=301,L]
RewriteRule ^consultoria-estrutural/?$ https://www.hartto.com/ [R=301,L]
RewriteRule ^cases/?$ https://www.hartto.com/portfolio/ [R=301,L]
RewriteRule ^projeto-estrutural-vitoria-es/?$ https://www.hartto.com/onde-atuamos/ [R=301,L]
# /index.html é duplicata da home: 301 (só para requisição externa, sem loop interno)
RewriteCond %{THE_REQUEST} \s/index\.html[\s?] [NC]
RewriteRule ^index\.html$ https://www.hartto.com/ [R=301,L]
# força https+www em UM salto (NC-22)
RewriteCond %{HTTPS} off [OR]
RewriteCond %{HTTP_HOST} ^hartto\.com$ [NC]
RewriteRule ^(.*)$ https://www.hartto.com/$1 [R=301,L]
# cabeçalhos de segurança (NC-05 parcial; CSP Report-Only e HSTS ficam para a fase 2)
<IfModule mod_headers.c>
Header always set X-Content-Type-Options "nosniff"
Header always set Referrer-Policy "strict-origin-when-cross-origin"
Header always set Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=()"
Header always set X-Frame-Options "DENY"
</IfModule>
# cache de assets
<IfModule mod_expires.c>
ExpiresActive On
ExpiresByType image/jpeg "access plus 30 days"
ExpiresByType image/png "access plus 30 days"
ExpiresByType text/css "access plus 7 days"
ExpiresByType application/javascript "access plus 7 days"
ExpiresByType font/ttf "access plus 365 days"
</IfModule>
